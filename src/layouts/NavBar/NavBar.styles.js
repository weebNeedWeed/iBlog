import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  text: {
    color: "black",
    fontWeight: 900,
    letterSpacing: "10px",
    marginBottom: "9px",
    fontSize: "700%",
  },
});

export default useStyles;
